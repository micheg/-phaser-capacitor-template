import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
	appId: "com.example.phasercapacitor",
	appName: "PhaserCapacitor",
	webDir: "dist",
};

export default config;
